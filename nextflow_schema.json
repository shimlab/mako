{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com///nextflow_schema.json",
  "title": " pipeline parameters",
  "description": "",
  "type": "object",
  "$defs": {
    "input_output_options": {
      "title": "Input/output options",
      "type": "object",
      "fa_icon": "fas fa-terminal",
      "description": "Define where the pipeline should find input data and save output data.",
      "required": ["dataset_name", "samplesheet", "outdir"],
      "properties": {
        "dataset_name": {
          "type": "string",
          "description": "The name of this dataset, used in output files and within visualisations"
        },
        "samplesheet": {
          "type": "string",
          "description": "Path to comma-separated file containing information about the samples in the experiment.",
          "format": "file-path"
        },
        "outdir": {
          "type": "string",
          "format": "directory-path",
          "description": "The output directory where the results will be saved. You have to use absolute paths to storage on Cloud infrastructure.",
          "fa_icon": "fas fa-folder-open"
        },
        "publish_dir_mode": {
          "type": "string",
          "default": "copy",
          "enum": ["copy", "symlink"],
          "description": "How to copy output from the work directory to the output directory"
        }
      }
    },
    "pipeline_parameters": {
      "title": "Pipeline parameters",
      "type": "object",
      "description": "",
      "default": "",
      "properties": {
        "differential_model": {
          "type": "string",
          "default": "adaptive_binomial",
          "description": "Model to use to call differentially modified sites, as a comma-separated list. Options: adaptive_binomial, homo_norm, hetero_norm, binomial, beta_binomial",
          "enum": [
            "adaptive_binomial",
            "homo_norm",
            "hetero_norm",
            "binomial",
            "beta_binomial"
          ]
        },
        "transcriptome": {
          "type": "string",
          "description": "Reference transcriptome, in .fasta format"
        },
        "gtf": {
          "type": "string",
          "description": "GTF annotation file"
        }
      },
      "required": ["differential_model", "transcriptome", "gtf"]
    },
    "modification_calling_parameters": {
      "title": "Modification calling parameters",
      "type": "object",
      "description": "Settings which control which sites are selected for differential calling.",
      "default": "",
      "properties": {
        "min_reads_per_sample": {
          "type": "integer",
          "default": 5,
          "description": "How many reads each sample should have at a site in order for the site to be considered."
        },
        "mod_threshold_dorado": {
          "type": "number",
          "description": "A passing threshold to use with Dorado data to determine a base modification.",
          "default": 0.5
        },
        "mod_filter_dorado": {
          "type": "string",
          "description": "Reads with a modification probability within this interval will be filtered out and not used in the Dorado model. The format should be `a,b` i.e. `0.2,0.8`. By default, no filtering is done.",
          "pattern": "^0\\.\\d+,0\\.\\d+$"
        },
        "mod_threshold_m6anet": {
          "type": "number",
          "description": "A passing threshold to use with m6Anet data to determine a base modification.",
          "default": 0.033379376
        },
        "mod_filter_m6anet": {
          "type": "string",
          "description": "Reads with a modification probability within this interval will be filtered out and not used in the m6Anet model. The format should be `a,b` i.e. `0.2,0.8`. By default, no filtering is done.",
          "pattern": "^0\\.\\d+,0\\.\\d+$"
        }
      },
      "required": [
        "min_reads_per_sample",
        "mod_threshold_dorado",
        "mod_threshold_m6anet"
      ]
    }
  },
  "allOf": [
    {
      "$ref": "#/$defs/input_output_options"
    },
    {
      "$ref": "#/$defs/pipeline_parameters"
    },
    {
      "$ref": "#/$defs/modification_calling_parameters"
    }
  ],
  "properties": {
    "testing": {
      "type": "boolean",
      "hidden": true,
      "description": "Only activated during stub runs"
    }
  }
}
